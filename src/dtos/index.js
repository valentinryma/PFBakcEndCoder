const CartDTO = require('./carts.dto');

const ProductDTO = require('./product.dto');

const UserDTO = require('./users.dto');

module.exports = {
    cart: CartDTO,
    product: ProductDTO,
    user: UserDTO
}